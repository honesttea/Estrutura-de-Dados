#include <stdio.h>
#include <stdlib.h>

int main(void){
    int n, m;
    scanf("%d", &n);
    scanf("%d", &m);

    int * vetor;
    vetor = (int*)malloc(sizeof(int)*n);

    for(int i=0;i<n;i++){
        n
    }
}